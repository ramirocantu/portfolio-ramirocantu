language: node_js
node_js:
  - "stable"
cache:
  directories:
  - node_modules
  yarn: true
before_script:
  - "yarn global add gatsby"
  - "yarn install"
script:
  - "npm run test"
deploy:
  provider: script
  script: "npm run deploy"
  on:
    branch: release
